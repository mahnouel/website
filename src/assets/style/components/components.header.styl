.header 
  border-radius: 0.75rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 2rem;


.header__title 
  font-size: 1.25em;
  color: inherit;
  text-decoration: none;
  border-radius: 0.75rem;
  h1 {
      font-size: inherit;
      font-weight: inherit;
      margin:0;
  }
